export interface Product {
    id: string
    nome: string
    preco: number
    image: string
}

export interface Informations {
    name: string
    telephone: string
    email: string
    address: string
}